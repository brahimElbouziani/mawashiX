"use client";

import { HeroAnimalCarousel } from "@/components/landing/hero-animal-carousel";

export function HeroVisual() {
  return (
    <div className="flex w-full flex-col items-center lg:w-full lg:items-center">
      <HeroAnimalCarousel />
    </div>
  );
}
